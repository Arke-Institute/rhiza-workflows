{
  "label": "Text to KG Basic",
  "description": "Scatters multiple text entities (must have 'text' property populated), chunks them, and extracts knowledge graph entities. For raw files (PDFs, images, JPEGs), use the corresponding file-to-KG workflow instead.",
  "version": "3.0",
  "entry": "scatter",
  "flow": {
    "scatter": {
      "klados": { "id": "$SCATTER_KLADOS" },
      "then": { "scatter": "chunk" }
    },
    "chunk": {
      "klados": { "id": "$TEXT_CHUNKER_KLADOS" },
      "then": { "scatter": "kg_process" }
    },
    "kg_process": {
      "klados": { "id": "$KG_BASIC_SINGLE_RHIZA", "type": "rhiza" },
      "then": { "done": true }
    }
  }
}
