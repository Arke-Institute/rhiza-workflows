{
  "label": "Images to OCR",
  "description": "Scatters multiple non-JPEG images (TIFF, PNG, WebP, GIF, BMP), converts each to JPEG, and extracts text via OCR. For JPEGs use jpegs-to-ocr.",
  "version": "1.0",
  "entry": "scatter",
  "flow": {
    "scatter": {
      "klados": { "id": "$SCATTER_KLADOS" },
      "then": { "scatter": "convert" }
    },
    "convert": {
      "klados": { "id": "$IMAGE_CONVERTER_KLADOS" },
      "then": { "pass": "ocr" }
    },
    "ocr": {
      "klados": { "id": "$OCR_WORKER_KLADOS" },
      "then": { "done": true }
    }
  }
}
